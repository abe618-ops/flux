name: Android Codex Bridge APK

on:
  workflow_dispatch:
  push:
    branches: [android-codex-bridge]
    paths:
      - 'android-codex-bridge/android-app/**'
      - '.github/workflows/android-codex-bridge-apk.yml'

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-codex-bridge/android-app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle --stacktrace assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-codex-bridge-debug-apk
          path: android-codex-bridge/android-app/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
