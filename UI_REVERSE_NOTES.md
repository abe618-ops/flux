# 紫微星语 APK：文王八卦 / 灵棋经界面解析记录

来源：用户提供的 `紫微星语.apk`（package `com.example.ziweixingyu`，versionName 1.8.29）。

## 文王八卦
原 Activity：`com.example.ziweixingyu.wenwangbagua`

原布局核心顺序：
1. 左上“返回”
2. 中间标题，24sp、粗体、文字色 #018786
3. 右上“爻变”
4. 标题下 5dp 为一张 235dp 高的摇三枚硬币说明图
5. 图下：“随机” + 122dp 数字输入框（原默认 202202） + “起课”
6. 下方多行结果框，白底、顶部左对齐、不可编辑、可滚动

摇卦说明图文字确认：“将三枚硬币摇摇 看字面的各个数 / 如上为2，重复6次依次填入下方”。
因此本版按每次三枚硬币出现“字面”的数量 0–3 记录六次，并用字面=2、另一面=3恢复 6/7/8/9 爻值。

## 灵棋经
原 Activity：`com.example.ziweixingyu.lingqijing`

原布局核心顺序：
1. 左上“返回”
2. 中间“灵棋经”，24sp、粗体、#018786
3. 右上“查询”
4. 三行四列共 12 枚棋位，初始均显示“空”；文字 24sp、粗体、#018786
5. 每行首格左边距约 30dp，行距约 15dp
6. 第三行下方左侧“随机”（24sp）
7. 下方多行结果框

DEX 中确认单字状态包含“上 / 中 / 下 / 空”。125 种结果按 000–444 三位键组织，所以本版将第一行映射为上棋、第二行为中棋、第三行为下棋；每行出现数为 0–4，三行计数组成查询键。

## 本版设计
主屏按用户要求改为一个纵向界面：
- 上：文王八卦
- 下：灵棋经
- 最底部仅保留一条紧凑的足球实验合参结果，不插入紫微十二宫大盘，避免破坏两个原界面的视觉结构。

本版是基于用户提供 APK 的互操作/研究性解析和 clean-room 重构。未直接复用原应用闭源 native 库。
